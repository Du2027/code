using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_Script : MonoBehaviour
{   /*
    public int speed;
    private GameObject Player;
    private Rigidbody Rigidbody_Player;
    private Vector3 Player_pos;
    private Vector3 Cam_pos;
    private GameObject cam;
    public int offset;
    private float pos_z = 0.0f;
    public int horizontal_speed;

    void Start()
    {
        Player = GameObject.Find("Player");
        Rigidbody_Player = Player.GetComponent<Rigidbody>();


        cam = GameObject.Find("Main Camera");
        //Cam_pos = cam.transform.position;
    }

    void FixedUpdate()
    {   
        //Player_pos = Player.transform.position;

        Rigidbody_Player.AddForce(new Vector3(0,0,-speed));

        
        //pos_z = Player_pos.z +offset;
        //cam.transform.position = new Vector3(Cam_pos.x,Cam_pos.y,pos_z);   
        string alla = null;

        bool a = Input.GetKey("d");

        if (a){
            //Rigidbody_Player.AddForce(-horizontal_speed,0,0);
            alla = "deez";
           Debug.Log(alla);
        }
        else {
            alla = "nothing";
            Debug.Log(alla);
        }


        

        if (Input.GetKey("a")){
            Rigidbody_Player.AddForce(new Vector3(horizontal_speed,0,0));
        }
    }

    public void addforce(){
    }
    */
}
