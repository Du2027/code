#include<stdio.h>
#include<stdbool.h>
int main(){
    bool true_false = false;
    printf("%d",true_false)     // %d specifier because bools are replaced with 1 for true and 0 for false
    return 0;
}